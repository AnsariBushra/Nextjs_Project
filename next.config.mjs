/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cdn.pixabay.com",
      "media.istockphoto.com",
      "www.istockphoto.com",
    ],
  },
};

export default nextConfig;
